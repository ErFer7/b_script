# b_script

A simple automation tool

---

Run `bash b_script.sh` to install, then run `pec setup` to se

---

## Commands:

`pec setup`

`pec build`

`pec resetbase`

`pec rebuild`

`pec run`

`pec front`